exclude:
  # Add more folders/files as regular expressions to exclude from Sigrid analysis.
  - '^testing(/|$)' # Excludes the entire testing/ folder
  - '^common(/|$)' # Excludes the entire common/ folder
  - "^\\.vscode(/|$)" # Excludes the entire .vscode/ folder
  - '^jest-cache(/|$)' # Excludes all jest testing caches
  - '^logs(/|$)' # Excludes all log files
  - ".*\\.json$" # Excludes all JSON files
  - "^\\.devcontainer(/|$)"
  - "^\\.github(/|$)"
  - '^docs(/|$)'
  - '^assets(/|$)'
  - '^node_modules(/|$)'
  - '^tools(/|$)'
  - '^tests(/|$)'
  - '^trials(/|$)' # Excludes experimental/demo files

languages:
  - name: TypeScript
dependencychecker:
  blocklist: ['NONE']
thirdpartyfindings:
  enabled: true
